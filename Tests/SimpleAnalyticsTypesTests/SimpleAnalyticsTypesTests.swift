import XCTest
@testable import SimpleAnalyticsTypes

final class SimpleAnalyticsTypesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SimpleAnalyticsTypes().text, "Hello, World!")
    }
}
